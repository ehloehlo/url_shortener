name: url_shortener

services:
  pg:
    image: postgres:16.3
    environment:
      POSTGRES_PASSWORD: postgres
    env_file:
      - .envrc
    volumes:
      - pg:/data/db
    ports:
      - 5432:5432

volumes:
  pg:
